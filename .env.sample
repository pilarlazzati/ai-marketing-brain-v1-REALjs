# Runtime
PORT=3000
USE_MOCK=true        # true = safe demo mode (no paid calls)

# OpenAI (only needed when USE_MOCK=false)
OPENAI_API_KEY=your-api-key-here
OPENAI_JSON_MODEL=gpt-4o-mini

# Public base for links (Replit will inject the URL at runtime, localhost ok for dev)
PUBLIC_BASE_URL=http://localhost:3000
